/**
 * Acquisition: source bundle discovery, the versioned list/get/update process
 * protocol, and source sandboxing. Owns the source-protocol contract seam
 * (spec §9.3).
 */
export const MODULE = "acquisition";
