/**
 * Shell: commander wiring, routing, rendering, and error-to-exit-code mapping
 * (spec §9.3). Placeholder entry point until the CLI surface lands.
 */
export function main(): void {
  process.exitCode = 0;
}

main();
