/**
 * Sandbox: a pure library — wrap(command, policy) → command; srt mechanics
 * hidden, no lifecycle ownership. Core-only by decision, nothing pluggable
 * (spec §9.3).
 */
export const MODULE = "sandbox";
